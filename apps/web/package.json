{
  "name": "@openclaw-team-os/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @openclaw-team-os/web'",
    "test": "echo 'No tests yet for @openclaw-team-os/web'"
  },
  "dependencies": {
    "@openclaw-team-os/config": "workspace:*",
    "@openclaw-team-os/domain": "workspace:*",
    "@openclaw-team-os/sdk": "workspace:*",
    "@openclaw-team-os/ui": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "vite": "^7.0.6"
  }
}
